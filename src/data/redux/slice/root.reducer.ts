import authenticationReducer from 'src/data/redux/slice/auth'

/**
 * @file rootReducer this is the birth 👨🏾‍🍼 place of reducers 😁
 *
 */

const rootReducer = {
  auth: authenticationReducer,
}

export default rootReducer
